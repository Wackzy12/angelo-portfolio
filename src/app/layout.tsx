import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Angelo Ramos | AI Developer",
  description:
    "Portfolio of Angelo Ramos, a fresh graduate aspiring to build a career in artificial intelligence and machine learning.",
  openGraph: {
    title: "Angelo Ramos | AI Developer",
    description:
      "Portfolio showcasing AI, machine learning, computer vision, and software development projects.",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
